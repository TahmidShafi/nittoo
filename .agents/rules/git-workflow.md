# Git Workflow Rules

- **Never push to Git without explicit user confirmation:** Do not run `git push` or push changes to remote git repositories without first asking the user and obtaining their explicit approval.
